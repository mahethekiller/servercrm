# Workspace Rules

- **Database Reference**: Always document database details in `db_info.md`. If any changes are made to the database schema or default values, update the schema definitions in `db_info.md` immediately to avoid querying the database repeatedly.
